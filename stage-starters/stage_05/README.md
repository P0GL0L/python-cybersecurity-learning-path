# Stage 5 - Testing, Packaging, CI, and Documentation

## Goal
Convert one prior stage project into an installable package; CI pipeline that runs tests and produces artifacts

## Acceptance Criteria
- >= 10 unit tests with meaningful assertions; README includes install, usage, examples, and troubleshooting

## How to Run
```bash
python app/main.py --help
```
